server:
  error:
    include-message: always
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/soundcloud_clone
    username: quan
    password: Vamario256
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
          facebook:
            client-id:
            client-secret:
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

aws:
  region: ap-southeast-1
  s3:
    buckets:
      customer: vamirio-soundcloud-clone